# Environment variable template for Group Interview Arena.
#
# P0-1 has no application code and therefore defines no runtime variables.
# Never place real secrets in this file or commit a populated `.env` file.
# Add documented placeholder names only when an approved implementation task
# introduces a concrete runtime dependency.
