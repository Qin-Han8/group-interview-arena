# Environment variable template for Group Interview Arena.
# Never place real secrets in this file or commit a populated `.env` file.

# P0-4B local PostgreSQL placeholders. Replace the password in your ignored
# local `.env`; these values are not production credentials.
POSTGRES_DB=group_interview_arena
POSTGRES_USER=group_interview_arena
POSTGRES_PASSWORD=<local-development-password>

# P0-4C server-only local development URL. Keep the password placeholder in
# tracked files, replace it only in ignored local configuration, and never use
# NEXT_PUBLIC_* for database credentials.
GIA_API_DATABASE_URL=postgresql+psycopg://group_interview_arena:<local-development-password>@127.0.0.1:5432/group_interview_arena

# P0-3E local FastAPI settings. CORS remains disabled unless explicitly set.
GIA_API_ENVIRONMENT=development
GIA_API_LOG_LEVEL=INFO
GIA_API_CORS_ORIGINS=["http://localhost:3000"]

# P0-5C server-only Cookie transport flag. Keep false only for local HTTP;
# production HTTPS must set this to true.
GIA_API_SESSION_COOKIE_SECURE=false

# Public browser configuration. Never place secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
